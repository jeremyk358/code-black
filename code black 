<html>
</H1>
<npm create vite@latest personal-project-showcase -- --template react
cd personal-project-showcase
npm install
npm run dev>
<personal-project-showcase/
├── src/
│   ├── App.jsx
│   ├── App.css
│   ├── index.css
│   └── main.jsx
├── public/
├── index.html
├── package.json
└─- vite.config.js>
<import { useState } from "react";
import "./App.css";

const projects = [
  {
    id: 1,
    title: "E-Commerce Dashboard",
    description:
      "A responsive dashboard for managing products, orders, customers, and sales analytics.",
    technologies: ["React", "JavaScript", "CSS"],
    category: "Web App",
    icon: "🛒",
    color: "grey",
    github: "https://github.com/",
  },
  {
    id: 2,
    title: "Weather Application",
    description:
      "A clean weather application that displays current conditions and forecasts for different cities.",
    technologies: ["React", "API", "CSS"],
    category: "Frontend",
    icon: "☀️",
    color: "orange",
    github: "https://github.com/",
  },
  {
    id: 3,
    title: "Task Manager",
    description:
      "A productivity application for creating, organizing, and tracking daily tasks.",
    technologies: ["React", "JavaScript", "LocalStorage"],
    category: "Web App",
    icon: "✅",
    color: "blue",
    github: "https://github.com/",
  },
  {
    id: 4,
    title: "Movie Explorer",
    description:
      "A movie discovery application that allows users to search for movies and explore popular titles.",
    technologies: ["React", "API", "JavaScript"],
    category: "Frontend",
    icon: "🎬",
    color: "purple",
    github: "https://github.com/",
  },
  {
    id: 5,
    title: "Portfolio Website",
    description:
      "A modern personal portfolio website designed to showcase development projects and skills.",
    technologies: ["React", "CSS", "Vite"],
    category: "Website",
    icon: "💻",
    color: "pink",
    github: "https://github.com/",
  },
  {
    id: 6,
    title: "Finance Tracker",
    description:
      "A personal finance application for tracking income, expenses, and monthly spending.",
    technologies: ["React", "JavaScript", "CSS"],
    category: "Web App",
    icon: "💰",
    color: "teal",
    github: "https://github.com/",
  },
];

const skills = [
  "React",
  "JavaScript",
  "HTML5",
  "CSS3",
  "Git & GitHub",
  "REST APIs",
  "Responsive Design",
  "Vite",
];

function App() {
  const [darkMode, setDarkMode] = useState(false);
  const [filter, setFilter] = useState("All");

  const filteredProjects =
    filter === "All"
      ? projects
      : projects.filter((project) => project.category === filter);

  const scrollToSection = (id) => {
    document.getElementById(id)?.scrollIntoView({
      behavior: "smooth",
    });
  };

  return (
    <div className={darkMode ? "app dark" : "app"}>
      {/* Navigation */}
      <header className="navbar">
        <div className="nav-container">
          <button
            className="logo"
            onClick={() => scrollToSection("home")}
          >
            <span>&lt;</span>Alex<span>/&gt;</span>
          </button>

          <nav>
            <button onClick={() => scrollToSection("home")}>Home</button>
            <button onClick={() => scrollToSection("about")}>About</button>
            <button onClick={() => scrollToSection("projects")}>
              Projects
            </button>
            <button onClick={() => scrollToSection("skills")}>Skills</button>
            <button onClick={() => scrollToSection("contact")}>Contact</button>
          </nav>

          <button
            className="theme-button"
            onClick={() => setDarkMode(!darkMode)}
            aria-label="Toggle dark mode"
          >
            {darkMode ? "☀️" : "🌙"}
          </button>
        </div>
      </header>

      <main>
        {/* Hero */}
        <section id="home" className="hero">
          <div className="hero-content">
            <p className="eyebrow">WELCOME TO MY PORTFOLIO</p>

            <h1>
              Building digital experiences
              <span> that make an impact.</span>
            </h1>

            <p className="hero-description">
              I'm Alex, a passionate React developer who enjoys building
              beautiful, responsive, and user-friendly web applications.
            </p>

            <div className="hero-buttons">
              <button
                className="primary-button"
                onClick={() => scrollToSection("projects")}
              >
                View My Projects →
              </button>

              <button
                className="secondary-button"
                onClick={() => scrollToSection("contact")}
              >
                Get In Touch
              </button>
            </div>

            <div className="social-links">
              <a href="https://github.com/" target="_blank" rel="noreferrer">
                GitHub
              </a>
              <a
                href="https://linkedin.com/"
                target="_blank"
                rel="noreferrer"
              >
                LinkedIn
              </a>
              <a href="mailto:hello@example.com">Email</a>
            </div>
          </div>

          <div className="hero-card">
            <div className="code-window">
              <div className="window-header">
                <span className="dot red"></span>
                <span className="dot yellow"></span>
                <span className="dot green"></span>
              </div>

              <div className="code">
                <p>
                  <span className="purple">const</span>{" "}
                  <span className="blue">developer</span> = {"{"}
                </p>
                <p className="indent">
                  name: <span className="green">"Alex"</span>,
                </p>
                <p className="indent">
                  role: <span className="green">"React Developer"</span>,
                </p>
                <p className="indent">
                  passion: <span className="green">"Building"</span>,
                </p>
                <p className="indent">
                  coffee: <span className="orange">true</span>
                </p>
                <p>{"};"}</p>
                <br />
                <p>
                  <span className="purple">export default</span> developer;
                </p>
              </div>
            </div>
          </div>
        </section>

        {/* About */}
        <section id="about" className="section about">
          <div className="section-heading">
            <p className="eyebrow">ABOUT ME</p>
            <h2>A little bit about me</h2>
          </div>

          <div className="about-grid">
            <div className="about-text">
              <p>
                I'm a web developer focused on creating modern digital
                experiences using React and JavaScript.
              </p>

              <p>
                I enjoy turning ideas into functional applications that are
                easy to use, visually appealing, and accessible across
                different devices.
              </p>

              <p>
                When I'm not coding, I enjoy learning new technologies,
                exploring UI design, and working on personal projects.
              </p>
            </div>

            <div className="about-stats">
              <div className="stat">
                <strong>10+</strong>
                <span>Projects Built</span>
              </div>

              <div className="stat">
                <strong>2+</strong>
                <span>Years Learning</span>
              </div>

              <div className="stat">
                <strong>8</strong>
                <span>Technologies</span>
              </div>

              <div className="stat">
                <strong>100%</strong>
                <span>Passion</span>
              </div>
            </div>
          </div>
        </section>

        {/* Projects */}
        <section id="projects" className="section projects-section">
          <div className="section-heading">
            <p className="eyebrow">MY WORK</p>
            <h2>Featured Projects</h2>
            <p>
              Here are some of the projects I've built while developing my
              skills.
            </p>
          </div>

          <div className="filters">
            {["All", "Web App", "Frontend", "Website"].map((item) => (
              <button
                key={item}
                className={filter === item ? "active" : ""}
                onClick={() => setFilter(item)}
              <
                {item}
              </button>
            ))}
          </div>

          <div className="project-grid">
            {filteredProjects.map((project) => (
              <article className="project-card" key={project.id}>
                <div className={`project-image ${project.color}`}>
                  <span>{project.icon}</span>
                </div>

                <div className="project-content">
                  <div className="project-category">
                    {project.category}
                  </div>

                  <h3>{project.title}</h3>

                  <p>{project.description}</p>

                  <div className="technology-list">
                    {project.technologies.map((technology) => (
                      <span key={technology}>{technology}</span>
                    ))}
                  </div>

                  <div className="project-links">
                    <a
                      href={project.github}
                      target="_blank"
                      rel="noreferrer"
                    >
                      GitHub ↗
                    </a>

                    <a
                      href={project.demo}
                      target="_blank"
                      rel="noreferrer"
                    >
                      Live Demo ↗
                    </a>
                  </div>
                </div>
              </article>
            ))}
          </div>
        </section>

        {/* Skills */}
        <section id="skills" className="section skills-section">
          <div className="section-heading">
            <p className="eyebrow">MY TOOLKIT</p>
            <h2>Skills & Technologies</h2>
            <p>
              Technologies and tools I use to bring ideas to life.
            </p>
          </div>

          <div className="skills-grid">
            {skills.map((skill, index) => (
              <div className="skill-card" key={skill}>
                <div className="skill-number">
                  {String(index + 1).padStart(2, "0")}
                </div>
                <h3>{skill}</h3>
              </div>
            ))}
          </div>
        </section>

        {/* Contact */}
        <section id="contact" className="section contact-section">
          <div className="contact-box">
            <p className="eyebrow">GET IN TOUCH</p>
            <h2>Let's build something together.</h2>
            <p>
              Have a project idea or want to talk about web development?
              I'd love to hear from you.
            </p>

            <a className="primary-button" href="mailto:hello@example.com">
              Send Me an Email →
            </a>
          </div>
        </section>
      </main>

      {/* Footer */}
      <footer>
        <div>
          <strong>&lt;Alex/&gt;</strong>
        </div>

        <p>© 2026 Alex. All rights reserved.</p>
      </footer>
    </div>
  ;
  * {
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  margin: 0;
  font-family:
    Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  background: #f8fafc;
  color: #172033;
}

button,
a {
  font: inherit;
}

button {
  cursor: pointer;
}

a {
  text-decoration: none;
}

.app {
  min-height: 100vh;
  background: #f8fafc;
  color: #172033;
  transition:
    background 0.3s ease,
    color 0.3s ease;
}

.app.dark {
  background: #0b1120;
  color: #f1f5f9;
}

/* Navbar */

.navbar {
  position: sticky;
  top: 0;
  z-index: 100;
  border-bottom: 1px solid #e2e8f0;
  background: rgba(248, 250, 252, 0.9);
  backdrop-filter: blur(14px);
}

.dark .navbar {
  border-color: #1e293b;
  background: rgba(11, 17, 32, 0.9);
}

.nav-container {
  width: min(1150px, 92%);
  height: 72px;
  margin: auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.logo {
  border: 0;
  background: transparent;
  color: #172033;
  font-size: 1.4rem;
  font-weight: 800;
}

.dark .logo {
  color: white;
}

.logo span {
  color: #6366f1;
}

nav {
  display: flex;
  gap: 28px;
}

nav button {
  border: 0;
  background: transparent;
  color: #64748b;
  font-weight: 600;
}

nav button:hover {
  color: #6366f1;
}

.theme-button {
  width: 42px;
  height: 42px;
  border: 1px solid #dbe2ea;
  border-radius: 50%;
  background: white;
}

.dark .theme-button {
  border-color: #334155;
  background: #172033;
}

/* Hero */

.hero {
  width: min(1150px, 92%);
  min-height: 680px;
  margin: auto;
  display: grid;
  grid-template-columns: 1.1fr 0.9fr;
  align-items: center;
  gap: 70px;
}

.eyebrow {
  margin-bottom: 14px;
  color: #6366f1;
  font-size: 0.8rem;
  font-weight: 800;
  letter-spacing: 2px;
}

.hero h1 {
  max-width: 750px;
  margin: 0;
  font-size: clamp(3rem, 6vw, 5.5rem);
  line-height: 0.98;
  letter-spacing: -4px;
}

.hero h1 span {
  color: #6366f1;
}

.hero-description {
  max-width: 620px;
  margin: 28px 0;
  color: #64748b;
  font-size: 1.15rem;
  line-height: 1.8;
}

.dark .hero-description,
.dark .section-heading p,
.dark .about-text p {
  color: #94a3b8;
}

.hero-buttons {
  display: flex;
  gap: 14px;
  flex-wrap: wrap;
}

.primary-button,
.secondary-button {
  display: inline-block;
  padding: 14px 22px;
  border-radius: 10px;
  border: 0;
  font-weight: 700;
  transition:
    transform 0.2s ease,
    box-shadow 0.2s ease;
}

.primary-button {
  color: white;
  background: #6366f1;
  box-shadow: 0 10px 25px rgba(99, 102, 241, 0.25);
}

.primary-button:hover,
.secondary-button:hover {
  transform: translateY(-3px);
}

.secondary-button {
  border: 1px solid #dbe2ea;
  color: #172033;
  background: white;
}

.dark .secondary-button {
  border-color: #334155;
  color: white;
  background: #172033;
}

.social-links {
  display: flex;
  gap: 22px;
  margin-top: 28px;
}

.social-links a {
  color: #64748b;
  font-weight: 600;
}

.social-links a:hover {
  color: #6366f1;
}

/* Code window */

.hero-card {
  animation: float 5s ease-in-out infinite;
}

.code-window {
  overflow: hidden;
  border: 1px solid #293449;
  border-radius: 18px;
  background: #111827;
  box-shadow: 0 30px 80px rgba(15, 23, 42, 0.25);
}

.window-header {
  display: flex;
  gap: 7px;
  padding: 15px 18px;
  border-bottom: 1px solid #273244;
}

.dot {
  width: 11px;
  height: 11px;
  border-radius: 50%;
}

.red {
  background: #f87171;
}

.yellow {
  background: #fbbf24;
}

.green {
  background: #34d399;
}

.code {
  padding: 35px;
  color: #dbeafe;
  font-family: "Courier New", monospace;
  line-height: 1.8;
}

.indent {
  padding-left: 25px;
}

.purple {
  color: #c084fc;
}

.blue {
  color: #60a5fa;
}

.code .green {
  color: #86efac;
}

.orange {
  color: #fb923c;
}

@keyframes float {
  0%,
  100% {
    transform: translateY(0);
  }

  50% {
    transform: translateY(-12px);
  }
}

/* Sections */

.section {
  width: min(1150px, 92%);
  margin: auto;
  padding: 100px 0;
}

.section-heading {
  max-width: 650px;
  margin-bottom: 45px;
}

.section-heading h2 {
  margin: 0 0 15px;
  font-size: clamp(2.2rem, 4vw, 3.5rem);
  letter-spacing: -2px;
}

.section-heading p:not(.eyebrow) {
  color: #64748b;
  line-height: 1.7;
}

/* About */

.about {
  border-top: 1px solid #e2e8f0;
}

.dark .about {
  border-color: #1e293b;
}

.about-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 80px;
  align-items: center;
}

.about-text p {
  color: #64748b;
  font-size: 1.08rem;
  line-height: 1.8;
}

.about-stats {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 15px;
}

.stat {
  padding: 28px;
  border: 1px solid #e2e8f0;
  border-radius: 15px;
  background: white;
}

.dark .stat {
  border-color: #263449;
  background: #111827;
}

.stat strong {
  display: block;
  margin-bottom: 8px;
  color: #6366f1;
  font-size: 2.3rem;
}

.stat span {
  color: #64748b;
}

/* Projects */

.projects-section {
  width: 100%;
  max-width: none;
  padding-left: max(4%, calc((100% - 1150px) / 2));
  padding-right: max(4%, calc((100% - 1150px) / 2));
  background: #f1f5f9;
}

.dark .projects-section {
  background: #0f172a;
}

.filters {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin-bottom: 35px;
}

.filters button {
  padding: 9px 18px;
  border: 1px solid #dbe2ea;
  border-radius: 30px;
  color: #64748b;
  background: white;
}

.dark .filters button {
  border-color: #334155;
  background: #172033;
}

.filters button.active {
  border-color: #6366f1;
  color: white;
  background: #6366f1;
}

.project-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 24px;
}

.project-card {
  overflow: hidden;
  border: 1px solid #e2e8f0;
  border-radius: 18px;
  background: white;
  transition:
    transform 0.25s ease,
    box-shadow 0.25s ease;
}

.dark .project-card {
  border-color: #263449;
  background: #111827;
}

.project-card:hover {
  transform: translateY(-8px);
  box-shadow: 0 25px 50px rgba(15, 23, 42, 0.12);
}

.project-image {
  height: 190px;
  display: grid;
  place-items: center;
}

.project-image span {
  font-size: 5rem;
}

.project-image.blue {
  background: #dbeafe;
}

.project-image.orange {
  background: #ffedd5;
}

.project-image.green {
  background: #dcfce7;
}

.project-image.purple {
  background: #f3e8ff;
}

.project-image.pink {
  background: #fce7f3;
}

.project-image.teal {
  background: #ccfbf1;
}

.project-content {
  padding: 25px;
}

.project-category {
  margin-bottom: 8px;
  color: #6366f1;
  font-size: 0.75rem;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 1px;
}

.project-content h3 {
  margin: 0 0 10px;
  font-size: 1.35rem;
}

.project-content p {
  min-height: 75px;
  color: #64748b;
  line-height: 1.6;
}

.technology-list {
  display: flex;
  flex-wrap: wrap;
  gap: 7px;
  margin: 20px 0;
}

.technology-list span {
  padding: 5px 9px;
  border-radius: 5px;
  color: #4f46e5;
  background: #eef2ff;
  font-size: 0.75rem;
  font-weight: 700;
}

.project-links {
  display: flex;
  justify-content: space-between;
  padding-top: 18px;
  border-top: 1px solid #e2e8f0;
}

.dark .project-links {
  border-color: #263449;
}

.project-links a {
  color: #475569;
  font-size: 0.9rem;
  font-weight: 700;
}

.dark .project-links a {
  color: #cbd5e1;
}

.project-links a:hover {
  color: #6366f1;
}

/* Skills */

.skills-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 15px;
}

.skill-card {
  padding: 28px;
  border: 1px solid #e2e8f0;
  border-radius: 14px;
  background: white;
  transition: 0.2s ease;
}

.dark .skill-card {
  border-color: #263449;
  background: #111827;
}

.skill-card:hover {
  border-color: #6366f1;
  transform: translateY(-4px);
}

.skill-number {
  margin-bottom: 25px;
  color: #a5b4fc;
  font-size: 0.8rem;
  font-weight: 800;
}

.skill-card h3 {
  margin: 0;
}

/* Contact */

.contact-section {
  padding-bottom: 120px;
}

.contact-box {
  padding: 75px 30px;
  border-radius: 25px;
  color: white;
  text-align: center;
  background: linear-gradient(135deg, #4f46e5, #7c3aed);
}

.contact-box h2 {
  max-width: 700px;
  margin: auto;
  font-size: clamp(2.3rem, 5vw, 4rem);
  letter-spacing: -2px;
}

.contact-box p:not(.eyebrow) {
  max-width: 600px;
  margin: 20px auto 35px;
  color: #e0e7ff;
  line-height: 1.7;
}

.contact-box .eyebrow {
  color: #c7d2fe;
}

.contact-box .primary-button {
  color: #4f46e5;
  background: white;
}

/* Footer */

footer {
  padding: 35px max(4%, calc((100% - 1150px) / 2));
  border-top: 1px solid #e2e8f0;
  display: flex;
  justify-content: space-between;
  gap: 20px;
  color: #64748b;
}

.dark footer {
  border-color: #1e293b;
}

footer div {
  display: flex;
  gap: 18px;
}

footer strong {
  color: #6366f1;
}

/* Responsive */

@media (max-width: 900px) {
  nav {
    display: none;
  }

  .hero {
    grid-template-columns: 1fr;
    padding: 80px 0;
  }

  .hero-card {
    max-width: 650px;
  }

  .project-grid {
    grid-template-columns: repeat(2, 1fr);
  }

  .skills-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (max-width: 650px) {
  .hero h1 {
    letter-spacing: -2px;
  }

  .about-grid {
    grid-template-columns: 1fr;
    gap: 40px;
  }

  .project-grid {
    grid-template-columns: 1fr;
  }

  .skills-grid {
    grid-template-columns: 1fr;
  }

  .about-stats {
    grid-template-columns: 1fr 1fr;
  }

  .code {
    padding: 20px;
    font-size: 0.75rem;
  }

  footer {
    flex-direction: column;
  }
}
:root {
  font-synthesis: none;
  text-rendering: optimizeLegibility;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

body {
  min-width: 320px;
}

::selection {
  color: white;
  background: #6366f1;
}
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import App from "./App.jsx";
import "./index.css";

createRoot(document.getElementById("root")).render(
  <StrictMode>
    <App />
  </StrictMode>
);
<npm run dev>
    <http://localhost 192.168. 0.1.>
    <name:"jeremy"
    <a href="https://github.com/YOUR_USERNAME">
  GitHub
</a>
<a href="mailto:YOUR_EMAILjeremykhadatorian@icloud.com">
  Send Me an Email 
</a>
